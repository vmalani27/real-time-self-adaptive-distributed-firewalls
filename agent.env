# Agent environment configuration (YAML to ENV mapping)
# From config.yaml:
# agents:
AGENT_ID=agent1
AGENT_IP=192.168.1.10
AGENT_REST_PORT=5001
ZEEK_LOG_PATH=/var/log/zeek/current/
SURICATA_LOG_PATH=/var/log/suricata/eve.json
API_KEY=changeme
LOG_DIR=../logs
# You can add CONTROLLER_URL, CONTROLLER_IP, CONTROLLER_ALERT_PORT if needed for agent/controller communication